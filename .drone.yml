---
kind: pipeline
name: linux-amd64

platform:
  os: linux
  arch: amd64

steps:
- name: fips-image-build-flannel
  image: docker:latest
  commands:
  - make
  when:
    event:
    - tag
    - push

- name: fips-image-push-flannel
  image: docker:latest
  commands:
  - make image-push

  when:
    event:
    - tag
    - push

- name: fips-image-manifest-flannel
  image: docker:latest
  commands:
  - make image-manifest
  when:
    event:
    - tag
    - push
